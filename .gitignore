# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/

# Data (downloaded benchmark corpora — large, not vendored)
data/
*.zip

# Benchmark outputs. Scratch is regenerable via `induce-text eval` and stays
# untracked; polished per-run reports and their figures are committed.
results/*
!results/reports/
